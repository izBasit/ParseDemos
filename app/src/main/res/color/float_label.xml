<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The color when activated/focused (usually your app's accent color) -->
    <item android:color="@color/orange_Faint" android:state_activated="true"/>
    <item android:color="@color/orange_Faint" android:state_focused="true"/>

    <!-- The color when not activated/focused (usually grey) -->
    <item android:color="@color/grey1"/>

</selector>